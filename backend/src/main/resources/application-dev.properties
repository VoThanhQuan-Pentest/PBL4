# Local fixtures are intentionally opt-in.  The default and production profiles
# resolve only the versioned schema migrations under db/migration.
spring.flyway.locations=classpath:db/migration,classpath:db/seed/dev
