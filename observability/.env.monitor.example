# Passwords are generated as mode-0600 files and Kibana/Logstash secure
# settings are stored in their product keystores. Compose never loads them as
# environment variables. This file contains only non-secret deployment knobs.
LOGSTASH_BIND_ADDRESS=10.20.10.20
